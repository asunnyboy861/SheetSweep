# SheetSweep — Clean Messy Spreadsheets Fast

**APP名称**: SheetSweep  
**副标题**: Clean Messy Spreadsheets Fast  
**App Store显示名**: SheetSweep - Spreadsheet Cleaner  
**报告日期**: 2026年5月2日  
**报告编号**: TR-20260502-供应商文件清理工具-操作指南

---

## APP命名深度分析

### 命名候选对比

| 候选名 | ASO搜索优化 | 品牌记忆度 | 功能描述性 | 情感共鸣 | 国际化适配 | 综合得分 |
|--------|------------|-----------|-----------|---------|-----------|---------|
| **SheetSweep** | 94 | 95 | 92 | 88 | 96 | **93.0** |
| CleanSheet | 85 | 82 | 88 | 75 | 90 | 84.0 |
| DataTidy | 78 | 78 | 85 | 65 | 88 | 78.8 |
| ExcelFix | 90 | 72 | 80 | 60 | 82 | 76.8 |
| VendorClean | 65 | 68 | 78 | 55 | 75 | 68.2 |

### SheetSweep 命名优势分析

**ASO搜索优化 (94分)**:
- "Sheet" 是App Store效率类目搜索量极高的关键词（spreadsheet/excel/sheet 月搜索量>80K）
- "Sweep" 暗示快速清扫/清理，与"clean/fix"语义强关联
- 组合词独特性极高，App Store无任何竞品占用该关键词
- App Store搜索算法对标题首词权重最高，"Sheet"排首位覆盖spreadsheet搜索意图
- 副标题"Clean Messy Spreadsheets Fast"覆盖3个高搜索量关键词：clean、messy、spreadsheets

**品牌记忆度 (95分)**:
- 双S头韵（SheetSweep），发音节奏感极强，一次听到终身记住
- Sweep意象鲜明——扫帚清扫，视觉联想极强
- 3音节（Sheet-Sweep），简短有力
- 视觉LOGO极易设计（扫帚清扫电子表格图标）
- 与同类名区分度极高（非CleanXxx/FixXxx等常见命名）

**功能描述性 (92分)**:
- Sheet = 电子表格，直接说明处理对象
- Sweep = 扫除/清理，暗示一键自动清理
- 组合含义："电子表格清扫器"，功能一目了然
- 副标题"Clean Messy Spreadsheets Fast"三重强化：清理+混乱表格+快速

**情感共鸣 (88分)**:
- Sweep暗示轻松、快速、彻底——"一扫而光"
- 消除数据混乱的焦虑感——"Sweep it clean"
- 对中小企业采购/财务人员有"救星工具"的信任感
- 与"spring cleaning"文化隐喻契合，美国用户极易共鸣

**国际化适配 (96分)**:
- Sheet全球通用（Google Sheets、Excel Sheet）
- Sweep全球通用，无文化歧义
- 无需翻译，全球市场直接使用
- 不含任何特定地区文化暗示

---

## 一、痛点深度研究与客户需求分析

### 1.1 核心痛点矩阵

| 痛点编号 | 痛点描述 | 严重程度 | 频率 | 付费驱动力 | 竞品解决度 |
|---------|---------|---------|------|-----------|-----------|
| P1 | 供应商Excel文件格式混乱，每次清理耗时半天 | 极高 | 每次收到供应商文件 | 极强 | 差 |
| P2 | 重复行/跨表重复，手动查找极易遗漏 | 极高 | 几乎每个文件 | 极强 | 差 |
| P3 | 日期格式不统一（MM/DD vs DD/MM vs 文本） | 高 | 几乎每个文件 | 强 | 一般 |
| P4 | 合并单元格破坏筛选和排序功能 | 高 | 70%的文件 | 强 | 差 |
| P5 | 列名不一致（Product ID vs SKU vs Code） | 高 | 跨供应商文件 | 强 | 极差 |
| P6 | 随机空格/隐藏字符破坏VLOOKUP查找 | 中高 | 几乎每个文件 | 中强 | 一般 |
| P7 | 混合货币/数字格式导致计算错误 | 中高 | 跨国供应商文件 | 中强 | 差 |
| P8 | 现有工具（Power Query/OpenRefine）技术门槛高 | 高 | 持续 | 强 | 极差 |
| P9 | 无移动端解决方案，外出无法处理紧急文件 | 中 | 偶发 | 中 | 极差 |
| P10 | 数据隐私担忧，不愿上传供应商数据到云端 | 中高 | 持续 | 中 | 一般 |

### 1.2 用户原话（痛点验证）

> "Every 'simple report' ends up becoming a half-day cleanup project before I can even upload to ERP/CRM. Duplicates across rows and sheets, dates in different formats, merged cells that break filters, random extra spaces that kill lookups."
> — Reddit r/smallbusiness

> "I know there are workarounds — Power Query, macros, even GPT — but in practice they're either too technical for most small teams, or not repeatable across multiple vendors."
> — Reddit r/smallbusiness

> "At my previous job, I would clean up data probably 30-60 minutes a day. I would average roughly a million new records a month. I had 2 main sources."
> — Reddit r/excel

> "80% of vendor sheets have data quality problems that cause ERP import failures and operational issues."
> — RowTidy Blog 数据

> "78% of companies struggle with messy vendor data that breaks ERP imports and causes procurement delays."
> — RowTidy Blog 数据

> "Our database of suppliers has 60,000 supplier sites, many of which (we have no idea how many) are duplicated."
> — MrExcel.com 论坛

> "Inconsistent data is more common, in that you are taking data from two sources and have to figure out ways to join them. They have different schemas and measure different things."
> — Reddit r/excel

### 1.3 痛点影响量化

| 影响维度 | 量化数据 |
|---------|---------|
| 时间浪费 | 每次清理平均2-4小时，每月8-16小时 |
| 人力成本 | 按$50/小时计算，每月$400-800浪费在数据清理 |
| ERP导入失败率 | 80%的供应商文件导致至少一次导入失败 |
| 重复供应商比例 | 平均15-25%的供应商记录为重复条目 |
| 决策延迟 | 数据清理导致采购决策平均延迟1-2天 |
| 错误成本 | 重复付款、重复订单平均每月造成$500-2000损失 |

### 1.4 客户需求优先级

| 优先级 | 需求 | 对应痛点 | MVP必须 |
|-------|------|---------|---------|
| P0 | 自动格式标准化（日期、货币、空格） | P3, P6, P7 | 是 |
| P0 | 跨表去重（精确+模糊匹配） | P2 | 是 |
| P0 | 列名规范化 | P5 | 是 |
| P0 | 导出干净Excel/CSV | P1 | 是 |
| P1 | 合并单元格自动拆分 | P4 | 是 |
| P1 | 供应商模板记忆（同一供应商下次自动应用规则） | P1 | 否（Phase 2） |
| P2 | 本地处理（隐私优先，不上传云端） | P10 | 是 |
| P2 | 批量文件处理 | P1 | 否（Phase 2） |
| P3 | 移动端快速预览和审批 | P9 | 否（Phase 3） |

---

## 二、GitHub可二次开发项目分析

### 2.1 核心依赖库

| 库名 | GitHub地址 | Stars | 功能 | 许可证 | 二次开发适合度 |
|------|-----------|-------|------|--------|--------------|
| CoreXLSX | CoreOffice/CoreXLSX | 850+ | Excel XLSX解析 | Apache 2.0 | 极高 |
| SwiftCSV | swiftcsv/SwiftCSV | 350+ | CSV解析 | MIT | 极高 |
| ZIPFoundation | weichsel/ZIPFoundation | 2200+ | ZIP解压（XLSX底层是ZIP） | MIT | 高 |
| SwiftDate | malcommac/SwiftDate | 7600+ | 日期格式识别和转换 | MIT | 极高 |

### 2.2 各库核心代码分析

#### CoreXLSX — Excel文件解析

**仓库地址**: https://github.com/CoreOffice/CoreXLSX  
**许可证**: Apache 2.0（允许商业使用）  
**核心能力**: 纯Swift实现的XLSX解析器，无需依赖libxml2

```swift
import CoreXLSX

class ExcelParser {
    func parseFile(at url: URL) throws -> SpreadsheetData {
        guard let spreadsheet = try? XLSXFile(filepath: url.path) else {
            throw ParseError.invalidFile
        }
        
        var sheets: [SheetData] = []
        
        for wbk in try spreadsheet.parseWorkbooks() {
            for (name, path) in try spreadsheet.parseWorksheetPathsAndNames(workbook: wbk) {
                guard let worksheet = try? spreadsheet.parseWorksheet(at: path) else { continue }
                
                var rows: [[String?]] = []
                let sharedStrings = try spreadsheet.parseSharedStrings()
                
                for row in worksheet.data?.rows ?? [] {
                    var cells: [String?] = []
                    for cell in row.cells {
                        let value = cell.stringValue(sharedStrings)
                        cells.append(value)
                    }
                    rows.append(cells)
                }
                
                sheets.append(SheetData(
                    name: name ?? "Sheet",
                    rows: rows,
                    columnCount: rows.first?.count ?? 0
                ))
            }
        }
        
        return SpreadsheetData(sheets: sheets, fileName: url.lastPathComponent)
    }
}
```

#### SwiftCSV — CSV文件解析

**仓库地址**: https://github.com/swiftcsv/SwiftCSV  
**许可证**: MIT（允许商业使用）  
**核心能力**: 轻量级CSV解析，支持自定义分隔符

```swift
import SwiftCSV

class CSVParser {
    func parseFile(at url: URL) throws -> SpreadsheetData {
        let csv = try CSV<Named>(url: url)
        
        var rows: [[String?]] = []
        for row in csv.rows {
            var cells: [String?] = []
            for header in csv.header {
                cells.append(row[header] ?? "")
            }
            rows.append(cells)
        }
        
        let sheetData = SheetData(
            name: "CSV Data",
            rows: rows,
            columnCount: csv.header.count
        )
        
        return SpreadsheetData(
            sheets: [sheetData],
            fileName: url.lastPathComponent
        )
    }
}
```

#### SwiftDate — 日期格式智能识别与转换

**仓库地址**: https://github.com/malcommac/SwiftDate  
**许可证**: MIT（允许商业使用）  
**核心能力**: 支持200+日期格式自动识别

```swift
import SwiftDate

class DateFormatDetector {
    static let commonFormats: [String] = [
        "MM/dd/yyyy", "dd/MM/yyyy", "yyyy-MM-dd",
        "MM-dd-yyyy", "dd-MM-yyyy", "MMM dd, yyyy",
        "dd MMM yyyy", "MM/dd/yy", "dd/MM/yy",
        "yyyyMMdd", "MM.dd.yyyy", "dd.MM.yyyy"
    ]
    
    func detectAndStandardize(_ value: String) -> String? {
        let trimmed = value.trimmingCharacters(in: .whitespaces)
        
        if let date = trimmed.dateWithFormats(commonFormats) {
            return date.toFormat("yyyy-MM-dd")
        }
        
        if let date = trimmed.toDate(nil, region: .UTC) {
            return date.toFormat("yyyy-MM-dd")
        }
        
        return nil
    }
}
```

### 2.3 XLSX写入方案

由于Swift生态缺乏成熟的XLSX写入库，采用自建轻量级XLSX生成器方案：

```swift
import ZIPFoundation

class XLSXWriter {
    func write(data: [[String?]], headers: [String], to url: URL) throws {
        let tempDir = FileManager.default.temporaryDirectory
            .appendingPathComponent(UUID().uuidString)
        try FileManager.default.createDirectory(at: tempDir, withIntermediateDirectories: true)
        
        defer { try? FileManager.default.removeItem(at: tempDir) }
        
        let contentTypes = """
        <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
        <Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types">
            <Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/>
            <Default Extension="xml" ContentType="application/xml"/>
            <Override PartName="/xl/workbook.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml"/>
            <Override PartName="/xl/worksheets/sheet1.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml"/>
            <Override PartName="/xl/sharedStrings.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sharedStrings+xml"/>
        </Types>
        """
        try contentTypes.write(to: tempDir.appendingPathComponent("[Content_Types].xml"), atomically: true, encoding: .utf8)
        
        var sharedStrings: [String: Int] = [:]
        var sharedStringsArray: [String] = []
        
        func getSharedStringIndex(_ s: String) -> Int {
            if let idx = sharedStrings[s] { return idx }
            sharedStrings[s] = sharedStringsArray.count
            sharedStringsArray.append(s)
            return sharedStringsArray.count - 1
        }
        
        var sheetXML = """
        <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
        <worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main">
        <sheetData>
        """
        
        for (rowIdx, row) in ([headers] + data).enumerated() {
            sheetXML += "<row r=\"\(rowIdx + 1)\">"
            for (colIdx, cell) in row.enumerated() {
                let colLetter = columnLetter(from: colIdx)
                let ref = "\(colLetter)\(rowIdx + 1)"
                if let value = cell, !value.isEmpty {
                    if let _ = Double(value) {
                        sheetXML += "<c r=\"\(ref)\"><v>\(value)</v></c>"
                    } else {
                        let idx = getSharedStringIndex(value)
                        sheetXML += "<c r=\"\(ref)\" t=\"s\"><v>\(idx)</v></c>"
                    }
                } else {
                    sheetXML += "<c r=\"\(ref)\"></c>"
                }
            }
            sheetXML += "</row>"
        }
        sheetXML += "</sheetData></worksheet>"
        try sheetXML.write(to: tempDir.appendingPathComponent("xl/worksheets/sheet1.xml"), atomically: true, encoding: .utf8)
        
        var ssXML = """
        <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
        <sst xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" count="\(sharedStringsArray.count)" uniqueCount="\(sharedStringsArray.count)">
        """
        for s in sharedStringsArray {
            let escaped = s.replacingOccurrences(of: "&", with: "&amp;")
                         .replacingOccurrences(of: "<", with: "&lt;")
                         .replacingOccurrences(of: ">", with: "&gt;")
            ssXML += "<si><t>\(escaped)</t></si>"
        }
        ssXML += "</sst>"
        try ssXML.write(to: tempDir.appendingPathComponent("xl/sharedStrings.xml"), atomically: true, encoding: .utf8)
        
        try FileManager.default.zipItem(at: tempDir, to: url)
    }
    
    private func columnLetter(from index: Int) -> String {
        var result = ""
        var idx = index
        while idx >= 0 {
            result = String(UnicodeScalar(65 + (idx % 26))!) + result
            idx = (idx / 26) - 1
        }
        return result
    }
}
```

---

## 三、核心技术实现代码示例与编写规则

### 3.1 数据模型设计

```swift
import SwiftData
import Foundation

@Model
final class CleaningSession {
    var id: UUID
    var fileName: String
    var importDate: Date
    var rowCount: Int
    var columnCount: Int
    var sheetCount: Int
    var issuesFound: Int
    var issuesFixed: Int
    var isCompleted: Bool
    var supplierName: String?
    var templateName: String?
    
    @Relationship(deleteRule: .cascade)
    var issues: [DataIssue]
    
    @Relationship(deleteRule: .cascade)
    var rules: [CleaningRule]
    
    @Relationship(deleteRule: .cascade)
    var columnMappings: [ColumnMapping]
    
    init(fileName: String) {
        self.id = UUID()
        self.fileName = fileName
        self.importDate = Date()
        self.rowCount = 0
        self.columnCount = 0
        self.sheetCount = 0
        self.issuesFound = 0
        self.issuesFixed = 0
        self.isCompleted = false
        self.issues = []
        self.rules = []
        self.columnMappings = []
    }
}

@Model
final class DataIssue {
    var id: UUID
    var issueType: IssueType
    var severity: IssueSeverity
    var sheetName: String
    var rowIndex: Int
    var columnIndex: Int
    var originalValue: String
    var suggestedFix: String
    var isResolved: Bool
    
    var session: CleaningSession?
    
    enum IssueType: String, Codable {
        case duplicateRow
        case inconsistentDateFormat
        case inconsistentCurrency
        case mergedCell
        case extraSpaces
        case hiddenCharacters
        case missingValue
        case inconsistentColumnName
        case invalidNumber
    }
    
    enum IssueSeverity: String, Codable {
        case critical
        case warning
        case info
    }
    
    init(type: IssueType, severity: IssueSeverity, sheet: String, row: Int, col: Int, original: String, fix: String) {
        self.id = UUID()
        self.issueType = type
        self.severity = severity
        self.sheetName = sheet
        self.rowIndex = row
        self.columnIndex = col
        self.originalValue = original
        self.suggestedFix = fix
        self.isResolved = false
    }
}

@Model
final class CleaningRule {
    var id: UUID
    var ruleType: RuleType
    var columnName: String
    var targetFormat: String
    var isActive: Bool
    
    var session: CleaningSession?
    
    enum RuleType: String, Codable {
        case dateFormat
        case currencyFormat
        case trimSpaces
        case removeHiddenChars
        case fillMissing
        case standardizeColumnName
    }
    
    init(type: RuleType, column: String, format: String) {
        self.id = UUID()
        self.ruleType = type
        self.columnName = column
        self.targetFormat = format
        self.isActive = true
    }
}

@Model
final class ColumnMapping {
    var id: UUID
    var originalName: String
    var standardName: String
    var confidence: Double
    
    var session: CleaningSession?
    
    init(original: String, standard: String, confidence: Double) {
        self.id = UUID()
        self.originalName = original
        self.standardName = standard
        self.confidence = confidence
    }
}

@Model
final class SupplierTemplate {
    var id: UUID
    var supplierName: String
    var createdAt: Date
    var lastUsed: Date
    var useCount: Int
    var columnMappings: [String: String]
    var cleaningRules: [String: String]
    
    init(supplierName: String) {
        self.id = UUID()
        self.supplierName = supplierName
        self.createdAt = Date()
        self.lastUsed = Date()
        self.useCount = 0
        self.columnMappings = [:]
        self.cleaningRules = [:]
    }
}
```

### 3.2 去重引擎（精确+模糊匹配）

```swift
import Foundation

struct DuplicateGroup {
    let primaryKey: String
    let rowIndices: [Int]
    let similarity: Double
    let suggestedKeep: Int
}

actor DeduplicationEngine {
    
    func findExactDuplicates(in data: [[String?]], columns: [Int]) -> [DuplicateGroup] {
        var groups: [String: [Int]] = [:]
        
        for (rowIdx, row) in data.enumerated() {
            let key = columns.map { colIdx in
                (row[colIdx] ?? "").lowercased().trimmingCharacters(in: .whitespaces)
            }.joined(separator: "|")
            
            groups[key, default: []].append(rowIdx)
        }
        
        return groups.filter { $0.value.count > 1 }.map { key, indices in
            DuplicateGroup(
                primaryKey: key,
                rowIndices: indices,
                similarity: 1.0,
                suggestedKeep: findMostCompleteRow(data: data, indices: indices)
            )
        }
    }
    
    func findFuzzyDuplicates(
        in data: [[String?]],
        columns: [Int],
        threshold: Double = 0.85
    ) -> [DuplicateGroup] {
        var groups: [DuplicateGroup] = []
        var processed = Set<Int>()
        
        for i in 0..<data.count {
            if processed.contains(i) { continue }
            
            var similarRows = [i]
            
            for j in (i+1)..<data.count {
                if processed.contains(j) { continue }
                
                let similarity = calculateRowSimilarity(
                    row1: data[i],
                    row2: data[j],
                    columns: columns
                )
                
                if similarity >= threshold {
                    similarRows.append(j)
                    processed.insert(j)
                }
            }
            
            if similarRows.count > 1 {
                processed.insert(i)
                groups.append(DuplicateGroup(
                    primaryKey: similarRows.map { String($0) }.joined(separator: ","),
                    rowIndices: similarRows,
                    similarity: threshold,
                    suggestedKeep: findMostCompleteRow(data: data, indices: similarRows)
                ))
            }
        }
        
        return groups
    }
    
    private func calculateRowSimilarity(
        row1: [String?],
        row2: [String?],
        columns: [Int]
    ) -> Double {
        var totalSimilarity = 0.0
        var validComparisons = 0
        
        for col in columns {
            let val1 = (row1[col] ?? "").trimmingCharacters(in: .whitespaces).lowercased()
            let val2 = (row2[col] ?? "").trimmingCharacters(in: .whitespaces).lowercased()
            
            guard !val1.isEmpty && !val2.isEmpty else { continue }
            
            totalSimilarity += jaroWinklerSimilarity(val1, val2)
            validComparisons += 1
        }
        
        return validComparisons > 0 ? totalSimilarity / Double(validComparisons) : 0
    }
    
    private func jaroWinklerSimilarity(_ s1: String, _ s2: String) -> Double {
        guard !s1.isEmpty && !s2.isEmpty else { return 0.0 }
        if s1 == s2 { return 1.0 }
        
        let searchRange = max(s1.count, s2.count) / 2 - 1
        let searchRangeClamped = max(0, searchRange)
        
        var s1Matches = Array(repeating: false, count: s1.count)
        var s2Matches = Array(repeating: false, count: s2.count)
        
        var matches = 0
        var transpositions = 0
        
        for (i, c1) in s1.enumerated() {
            let start = max(0, i - searchRangeClamped)
            let end = min(i + searchRangeClamped + 1, s2.count)
            
            for j in start..<end {
                guard !s2Matches[j] else { continue }
                let c2 = s2[s2.index(s2.startIndex, offsetBy: j)]
                if c1 == c2 {
                    s1Matches[i] = true
                    s2Matches[j] = true
                    matches += 1
                    break
                }
            }
        }
        
        guard matches > 0 else { return 0.0 }
        
        var k = 0
        for (i, matched) in s1Matches.enumerated() {
            guard matched else { continue }
            while !s2Matches[k] { k += 1 }
            let c1 = s1[s1.index(s1.startIndex, offsetBy: i)]
            let c2 = s2[s2.index(s2.startIndex, offsetBy: k)]
            if c1 != c2 { transpositions += 1 }
            k += 1
        }
        
        let jaro = (Double(matches) / Double(s1.count) +
                    Double(matches) / Double(s2.count) +
                    Double(matches - transpositions / 2) / Double(matches)) / 3.0
        
        let prefix = min(4, zip(s1, s2).prefix(while: { $0.0 == $0.1 }).count)
        let winkler = jaro + Double(prefix) * 0.1 * (1.0 - jaro)
        
        return winkler
    }
    
    private func findMostCompleteRow(data: [[String?]], indices: [Int]) -> Int {
        indices.max(by: { a, b in
            let countA = data[a].filter { ($0 ?? "").isEmpty == false }.count
            let countB = data[b].filter { ($0 ?? "").isEmpty == false }.count
            return countA < countB
        } ?? indices[0]
    }
}
```

### 3.3 格式标准化引擎

```swift
import Foundation

actor FormatStandardizer {
    
    private let dateFormats = [
        "MM/dd/yyyy", "dd/MM/yyyy", "yyyy-MM-dd",
        "MM-dd-yyyy", "dd-MM-yyyy", "MMM dd, yyyy",
        "dd MMM yyyy", "MM/dd/yy", "dd/MM/yy",
        "yyyyMMdd", "MM.dd.yyyy", "dd.MM.yyyy",
        "yyyy/MM/dd", "M/d/yyyy", "d/M/yyyy",
        "M/d/yy", "d/M/yy"
    ]
    
    func standardizeDate(_ value: String, targetFormat: String = "yyyy-MM-dd") -> String? {
        let trimmed = value.trimmingCharacters(in: .whitespaces)
        guard !trimmed.isEmpty else { return nil }
        
        let dateFormatter = DateFormatter()
        dateFormatter.locale = Locale(identifier: "en_US_POSIX")
        
        for format in dateFormats {
            dateFormatter.dateFormat = format
            if let date = dateFormatter.date(from: trimmed) {
                dateFormatter.dateFormat = targetFormat
                return dateFormatter.string(from: date)
            }
        }
        
        if let date = dateFormatter.date(from: trimmed) {
            dateFormatter.dateFormat = targetFormat
            return dateFormatter.string(from: date)
        }
        
        return nil
    }
    
    func standardizeCurrency(_ value: String) -> (amount: Double?, currency: String?) {
        let trimmed = value.trimmingCharacters(in: .whitespaces)
        guard !trimmed.isEmpty else { return (nil, nil) }
        
        let currencySymbols: [String: String] = [
            "$": "USD", "€": "EUR", "£": "GBP",
            "¥": "JPY", "₹": "INR", "CHF": "CHF",
            "A$": "AUD", "C$": "CAD"
        ]
        
        var detectedCurrency: String?
        var numericString = trimmed
        
        for (symbol, code) in currencySymbols.sorted(by: { $0.key.count > $1.key.count }) {
            if numericString.contains(symbol) {
                detectedCurrency = code
                numericString = numericString.replacingOccurrences(of: symbol, with: "")
                break
            }
        }
        
        let codePattern = "\\b(USD|EUR|GBP|JPY|INR|CHF|AUD|CAD)\\b"
        if detectedCurrency == nil,
           let regex = try? NSRegularExpression(pattern: codePattern),
           let match = regex.firstMatch(in: numericString, range: NSRange(numericString.startIndex..., in: numericString)),
           let range = Range(match.range, in: numericString) {
            detectedCurrency = String(numericString[range])
            numericString = numericString.replacingCharacters(in: range, with: "")
        }
        
        numericString = numericString
            .replacingOccurrences(of: ",", with: "")
            .replacingOccurrences(of: " ", with: "")
            .trimmingCharacters(in: .whitespaces)
        
        let amount = Double(numericString)
        return (amount, detectedCurrency)
    }
    
    func trimExtraSpaces(_ value: String) -> String {
        let components = value.components(separatedBy: .whitespaces)
        return components.filter { !$0.isEmpty }.joined(separator: " ")
    }
    
    func removeHiddenCharacters(_ value: String) -> String {
        value.unicodeScalars.filter { scalar in
            CharacterSet.whitespacesAndNewlines.contains(scalar) ||
            CharacterSet.alphanumerics.contains(scalar) ||
            CharacterSet.punctuationCharacters.contains(scalar) ||
            CharacterSet.symbols.contains(scalar)
        }.map(String.init).joined()
    }
}
```

### 3.4 列名规范化引擎

```swift
import Foundation

actor ColumnNameNormalizer {
    
    private let standardSchema: [String: [String]] = [
        "SKU": ["Product ID", "Item Code", "Part Number", "Product Code", "Item No", "SKU Code", "Product No", "Item Number", "Artikel-Nr", "Artikelnummer"],
        "Product_Name": ["Product Name", "Item Description", "Description", "Product Description", "Item Name", "Product Title", "Bezeichnung", "Artikelbezeichnung"],
        "Unit_Price": ["Price", "Unit Price", "Unit Cost", "Price/Unit", "Cost", "Unit Rate", "Einzelpreis", "Preis"],
        "Currency": ["Currency", "Curr", "CCY", "Währung"],
        "Quantity": ["Qty", "Quantity", "Amount", "Units", "Menge", "Anzahl"],
        "Category": ["Category", "Product Category", "Product Type", "Class", "Group", "Kategorie"],
        "Supplier": ["Supplier", "Vendor", "Supplier Name", "Vendor Name", "Lieferant"],
        "Date": ["Date", "Order Date", "Delivery Date", "Invoice Date", "Datum", "Bestelldatum"],
        "Total": ["Total", "Total Price", "Line Total", "Amount", "Gesamt", "Gesamtpreis"],
        "Stock": ["Stock", "Inventory", "Qty Available", "On Hand", "Bestand", "Lagerbestand"]
    ]
    
    func normalizeColumnNames(_ headers: [String]) -> [ColumnMapping] {
        headers.map { header in
            let cleaned = header
                .trimmingCharacters(in: .whitespaces)
                .lowercased()
                .replacingOccurrences(of: "_", with: " ")
                .replacingOccurrences(of: "-", with: " ")
            
            var bestMatch = header
            var bestConfidence = 0.0
            
            for (standard, variants) in standardSchema {
                for variant in variants {
                    let variantCleaned = variant.lowercased()
                    let similarity = stringSimilarity(cleaned, variantCleaned)
                    
                    if similarity > bestConfidence {
                        bestConfidence = similarity
                        bestMatch = standard
                    }
                }
            }
            
            return ColumnMapping(
                original: header,
                standard: bestConfidence >= 0.7 ? bestMatch : header,
                confidence: bestConfidence
            )
        }
    }
    
    private func stringSimilarity(_ s1: String, _ s2: String) -> Double {
        guard !s1.isEmpty && !s2.isEmpty else { return 0 }
        if s1 == s2 { return 1.0 }
        
        let set1 = Set(s1.map { $0 })
        let set2 = Set(s2.map { $0 })
        let intersection = set1.intersection(set2)
        let union = set1.union(set2)
        
        return Double(intersection.count) / Double(union.count)
    }
}
```

### 3.5 编写规则

| 规则编号 | 规则描述 | 示例 |
|---------|---------|------|
| R1 | 所有数据处理使用`actor`确保线程安全 | `actor DeduplicationEngine` |
| R2 | 使用Swift Concurrency（async/await）处理大文件 | `func parseFile() async throws` |
| R3 | SwiftData作为本地持久化方案 | `@Model final class CleaningSession` |
| R4 | 所有文件解析在后台线程执行 | `Task.detached { ... }` |
| R5 | 隐私优先：所有数据处理在本地完成，不发送到任何服务器 | 无网络请求代码 |
| R6 | 使用`@Observable`宏替代ObservableObject | `@Observable final class ViewModel` |
| R7 | SwiftUI + NavigationStack构建UI | `NavigationStack { ... }` |
| R8 | 错误处理使用Swift原生Error协议 | `enum ParseError: Error { case invalidFile }` |
| R9 | 大文件处理使用流式解析，避免一次性加载到内存 | 分块读取 |
| R10 | 支持Undo/Redo操作 | 使用`UndoManager` |

---

## 四、实现流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                    SheetSweep 实现流程图                          │
└─────────────────────────────────────────────────────────────────┘

Phase 1: MVP核心功能（3-4周）
═══════════════════════════════

Week 1: 文件解析层
─────────────────
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  XLSX文件     │───▶│  CoreXLSX    │───▶│  解析为      │
│  输入         │    │  解析引擎    │    │  SpreadsheetData │
└──────────────┘    └──────────────┘    └──────────────┘
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  CSV文件      │───▶│  SwiftCSV    │───▶│  解析为      │
│  输入         │    │  解析引擎    │    │  SpreadsheetData │
└──────────────┘    └──────────────┘    └──────────────┘

Week 2: 数据处理层
─────────────────
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  原始数据     │───▶│  问题检测    │───▶│  问题列表    │
│  SpreadsheetData│  │  引擎        │    │  DataIssue[] │
└──────────────┘    └──────────────┘    └──────────────┘
       │                                        │
       ▼                                        ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  去重引擎     │───▶│  精确匹配    │    │  模糊匹配    │
│  DedupEngine │    │  JaroWinkler │    │  阈值0.85    │
└──────────────┘    └──────────────┘    └──────────────┘
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  格式标准化   │───▶│  日期格式    │───▶│  统一输出    │
│  Standardizer│    │  货币格式    │    │  yyyy-MM-dd  │
│              │    │  空格清理    │    │  ISO 4217    │
└──────────────┘    └──────────────┘    └──────────────┘
┌──────────────┐    ┌──────────────┐
│  列名规范化   │───▶│  标准Schema  │
│  Normalizer  │    │  映射表      │
└──────────────┘    └──────────────┘

Week 3: UI展示层 + 导出层
─────────────────────────
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  问题预览     │───▶│  用户确认    │───▶│  执行修复    │
│  列表视图     │    │  逐条审核    │    │  应用规则    │
└──────────────┘    └──────────────┘    └──────────────┘
                                              │
                                              ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  XLSX导出    │◀───│  清洁数据    │◀───│  修复后数据  │
│  XLSXWriter  │    │  验证通过    │    │  合并结果    │
└──────────────┘    └──────────────┘    └──────────────┘
┌──────────────┐
│  CSV导出     │
│  原生写入    │
└──────────────┘

Week 4: 测试 + 优化 + 上架
──────────────────────────
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  单元测试     │───▶│  UI测试      │───▶│  App Store   │
│  去重/格式化  │    │  用户流程    │    │  提交审核    │
└──────────────┘    └──────────────┘    └──────────────┘
```

---

## 五、用户流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                    SheetSweep 用户流程图                          │
└─────────────────────────────────────────────────────────────────┘

┌─────────┐
│  启动App │
└────┬────┘
     │
     ▼
┌─────────────┐     ┌──────────────────────────────┐
│  首页/欢迎  │────▶│  点击 "+" 或拖拽文件导入      │
│  空状态     │     │  支持 .xlsx / .csv / .xls     │
└─────────────┘     └──────────────┬───────────────┘
                                   │
                                   ▼
                    ┌──────────────────────────────┐
                    │  文件解析中...                 │
                    │  显示进度条 + 文件基本信息     │
                    │  (行数/列数/表数)              │
                    └──────────────┬───────────────┘
                                   │
                                   ▼
                    ┌──────────────────────────────┐
                    │  📊 扫描结果概览               │
                    │  ┌────────────────────────┐  │
                    │  │ 🔴 重复行: 23条         │  │
                    │  │ 🟡 日期格式问题: 156处   │  │
                    │  │ 🟡 列名不规范: 4列      │  │
                    │  │ 🟠 合并单元格: 12处      │  │
                    │  │ 🔵 多余空格: 89处        │  │
                    │  │ 🟣 货币格式混合: 34处    │  │
                    │  └────────────────────────┘  │
                    │                               │
                    │  [一键修复全部]  [逐项审核]    │
                    └──────┬───────────┬───────────┘
                           │           │
              ┌────────────┘           └────────────┐
              ▼                                     ▼
┌────────────────────────┐          ┌────────────────────────┐
│  ⚡ 一键修复            │          │  🔍 逐项审核           │
│  自动应用所有建议修复    │          │  按类别查看问题详情     │
│  显示修复摘要           │          │                        │
└───────────┬────────────┘          │  ┌──────────────────┐  │
            │                        │  │ 重复行详情        │  │
            │                        │  │ 行A vs 行B        │  │
            │                        │  │ 相似度: 92%       │  │
            │                        │  │ [保留A] [保留B]   │  │
            │                        │  │ [合并AB]          │  │
            │                        │  └──────────────────┘  │
            │                        │                        │
            │                        │  ┌──────────────────┐  │
            │                        │  │ 日期格式详情      │  │
            │                        │  │ 原始: 03/15/2026  │  │
            │                        │  │ 修复: 2026-03-15  │  │
            │                        │  │ [确认] [修改]     │  │
            │                        │  └──────────────────┘  │
            │                        └───────────┬────────────┘
            │                                    │
            ▼                                    ▼
┌────────────────────────────────────────────────────────┐
│  ✅ 修复完成                                            │
│  ┌────────────────────────────────────────────────┐    │
│  │ 修复摘要:                                       │    │
│  │ ✅ 删除重复行: 23条                              │    │
│  │ ✅ 标准化日期: 156处                             │    │
│  │ ✅ 规范列名: 4列                                 │    │
│  │ ✅ 拆分合并单元格: 12处                          │    │
│  │ ✅ 清理空格: 89处                                │    │
│  │ ✅ 统一货币格式: 34处                            │    │
│  └────────────────────────────────────────────────┘    │
│                                                         │
│  [💾 导出XLSX]  [📄 导出CSV]  [📋 复制到剪贴板]         │
└──────────────────────────┬─────────────────────────────┘
                           │
                           ▼
               ┌────────────────────────┐
               │  选择导出方式           │
               │  • 保存到Files          │
               │  • 分享(AirDrop/邮件)   │
               │  • 保存到iCloud Drive   │
               └────────────────────────┘
```

---

## 六、软件数据流图

```
┌─────────────────────────────────────────────────────────────────┐
│                    SheetSweep 数据流图                            │
└─────────────────────────────────────────────────────────────────┘

外部实体
══════

┌──────────┐                                        ┌──────────┐
│   用户    │                                        │  文件系统  │
│  (User)  │                                        │ (Files)  │
└────┬─────┘                                        └─────┬────┘
     │                                                    │
     │  导入文件请求                                        │  读取文件
     │                                                    │
     ▼                                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                        处理进程                                   │
│                                                                   │
│  ┌─────────────┐    ┌──────────────┐    ┌──────────────┐        │
│  │  文件解析器  │───▶│  数据存储     │───▶│  问题检测器  │        │
│  │  FileParser │    │  DataStore   │    │  IssueScanner│        │
│  └─────────────┘    └──────────────┘    └──────────────┘        │
│         │                   │                    │                │
│         │                   │                    │                │
│         ▼                   ▼                    ▼                │
│  ┌─────────────┐    ┌──────────────┐    ┌──────────────┐        │
│  │ Spreadsheet │    │ CleaningSession│   │  DataIssue[] │        │
│  │ Data结构    │    │ SwiftData模型 │    │  问题列表    │        │
│  └─────────────┘    └──────────────┘    └──────────────┘        │
│                                                │                │
│                                                │                │
│                    ┌───────────────────────────┘                │
│                    │                                            │
│                    ▼                                            │
│  ┌──────────────────────────────────────────────────────┐      │
│  │                    数据处理引擎                        │      │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐           │      │
│  │  │ 去重引擎 │  │ 格式标准化│  │ 列名规范化│           │      │
│  │  │ Dedup   │  │ Format   │  │ Column   │           │      │
│  │  │ Engine  │  │ Standard │  │ Normal   │           │      │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘           │      │
│  │       │              │              │                  │      │
│  │       ▼              ▼              ▼                  │      │
│  │  ┌──────────────────────────────────────────┐        │      │
│  │  │          清洁数据合并器                    │        │      │
│  │  │      CleanDataMerger                     │        │      │
│  │  └──────────────────┬───────────────────────┘        │      │
│  └─────────────────────┼────────────────────────────────┘      │
│                        │                                         │
│                        ▼                                         │
│  ┌──────────────────────────────────────────────────────┐      │
│  │                    导出引擎                            │      │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐           │      │
│  │  │ XLSX写入 │  │ CSV写入  │  │ 剪贴板   │           │      │
│  │  │ Writer   │  │ Writer   │  │ Copy     │           │      │
│  │  └──────────┘  └──────────┘  └──────────┘           │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘

数据存储
══════

┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  SwiftData       │  │  SupplierTemplate │  │  UserDefaults    │
│  CleaningSession │  │  供应商模板记忆    │  │  用户偏好设置    │
│  DataIssue       │  │  ColumnMapping   │  │  默认格式偏好    │
│  CleaningRule    │  │  CleaningRule    │  │  去重阈值设置    │
│  ColumnMapping   │  │                  │  │                  │
└──────────────────┘  └──────────────────┘  └──────────────────┘

数据流向说明
══════════

1. 用户 → 文件解析器: 用户选择/拖拽文件
2. 文件系统 → 文件解析器: 读取XLSX/CSV原始数据
3. 文件解析器 → 数据存储: 存储解析后的结构化数据
4. 数据存储 → 问题检测器: 传入原始数据检测问题
5. 问题检测器 → 数据处理引擎: 传入问题列表和原始数据
6. 数据处理引擎 → 清洁数据合并器: 各引擎处理结果合并
7. 清洁数据合并器 → 导出引擎: 生成最终清洁数据
8. 导出引擎 → 文件系统: 写入XLSX/CSV文件
9. 导出引擎 → 用户: 分享/保存文件
10. 用户偏好 → 数据处理引擎: 影响处理规则和阈值
```

---

## 七、极具竞争优势的价格策略

### 7.1 竞品定价分析

| 竞品 | 平台 | 定价模式 | 价格 | 免费版 | 核心缺陷 |
|------|------|---------|------|--------|---------|
| RowTidy | Web | 订阅制 | $15/月(Starter) / $49/月(Pro) | 有限试用 | 无iOS原生App，需浏览器 |
| SheetCleaner | Web | 免费+付费 | 免费基础 / 付费高级 | 有 | 无iOS App，浏览器运行 |
| Power Query | Win/Mac | 免费(需Excel) | $0(需Office 365) | 有 | 技术门槛高，无移动端 |
| OpenRefine | 桌面 | 开源免费 | $0 | 完全免费 | 无移动端，学习曲线陡峭 |
| Alteryx | 桌面 | 企业订阅 | $500+/月 | 无 | 极贵，面向企业 |
| Clean Merge Contacts | iOS | 内购 | $8.99/周 / $29.99/月 / $49.99终身 | 有 | 仅清理联系人，非电子表格 |

### 7.2 SheetSweep定价策略：站内订阅B模式

**选择理由**: SheetSweep功能属于"功能稍微复杂"类别——涉及文件解析、去重算法、格式标准化、列名映射等多步骤处理，但无API生成费用（全部本地处理），因此采用**站内订阅B**模式。

#### 定价方案

```
┌─────────────────────────────────────────────────────────────┐
│                  SheetSweep 定价方案                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  🆓 Free — 免费下载 + 免费功能                                │
│  ─────────────────────────────                                │
│  • 每月3次文件清理                                            │
│  • 基础去重（精确匹配）                                       │
│  • 空格清理 + 隐藏字符移除                                    │
│  • CSV导出                                                   │
│  • 单文件处理（不支持批量）                                    │
│  • 最多1000行/文件                                            │
│                                                               │
│  💎 Pro — $4.99/月 或 $39.99/年（省33%）                      │
│  ─────────────────────────────                                │
│  • 无限文件清理次数                                           │
│  • 高级去重（精确+模糊匹配，JaroWinkler算法）                 │
│  • 日期格式自动标准化                                         │
│  • 货币格式统一                                               │
│  • 列名智能规范化                                             │
│  • 合并单元格自动拆分                                         │
│  • XLSX + CSV 双格式导出                                     │
│  • 无行数限制                                                 │
│  • 供应商模板记忆（保存清理规则）                              │
│                                                               │
│  🏢 Business — $9.99/月 或 $79.99/年（省33%）                 │
│  ─────────────────────────────                                │
│  • 所有Pro功能                                                │
│  • 批量文件处理（一次导入多个供应商文件）                      │
│  • 跨文件去重                                                 │
│  • 自定义标准Schema                                           │
│  • 清理报告导出（PDF）                                        │
│  • iCloud同步清理规则                                         │
│  • 优先技术支持                                               │
│                                                               │
│  ⭐ Lifetime — $79.99 一次性买断                              │
│  ─────────────────────────────                                │
│  • 等同Business版所有功能                                     │
│  • 一次付费永久使用                                           │
│  • 包含1年功能更新                                            │
│  • 注意：因无API费用，可提供一次性买断                         │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### 7.3 定价策略深度分析

#### 为什么不选"完全免费"模式？
- SheetSweep解决的是B2B刚需痛点（每月节省$400-800人力成本），用户付费意愿强
- 完全免费无法建立产品价值认知，用户会质疑"免费的能靠谱吗？"
- B2B工具免费模式难以转化付费用户

#### 为什么不选"下载前付费购买"模式？
- 用户需要先体验才能判断清理效果，下载前付费会大幅降低转化率
- 竞品RowTidy提供免费试用，不提供试用会流失用户
- App Store不支持"付费后下载"的退款体验优化

#### 为什么选"站内订阅B"模式？
- **功能复杂度匹配**: SheetSweep涉及多步骤处理（解析→检测→修复→导出），属于功能稍微复杂类别
- **无API费用**: 全部本地处理，无服务器/API成本，因此可以提供一次性买断选项
- **免费层引流**: 3次/月免费清理足够让用户体验核心价值，形成习惯后转化付费
- **月费$4.99 vs 竞品$15**: 价格仅为RowTidy的1/3，极具竞争力
- **年付折扣33%**: 鼓励长期订阅，降低流失率
- **一次性买断$79.99**: 满足不愿订阅的用户，同时设置合理上限

#### 定价心理学分析

| 策略 | 说明 |
|------|------|
| 锚定效应 | Business $9.99/月让Pro $4.99/月显得便宜 |
| 损失厌恶 | "每月3次免费"用完后产生损失感，驱动付费 |
| 沉没成本 | 用户在免费版投入时间学习，不愿换到竞品 |
| 社会认同 | Pro/Business标签暗示专业用户的选择 |
| 稀缺性 | 限时Lifetime买断创造紧迫感 |

#### 收入预测

| 场景 | 月下载量 | Free转化率 | Pro订阅 | Business订阅 | Lifetime | 月收入 |
|------|---------|-----------|---------|-------------|----------|--------|
| 保守 | 500 | 5% | 20人×$4.99 | 3人×$9.99 | 2人×$79.99 | ~$340 |
| 中等 | 2000 | 8% | 130人×$4.99 | 15人×$9.99 | 5人×$79.99 | ~$1,830 |
| 乐观 | 5000 | 12% | 480人×$4.99 | 50人×$9.99 | 10人×$79.99 | ~$5,460 |

#### 与竞品定价对比优势

| 维度 | SheetSweep | RowTidy | Power Query |
|------|-----------|---------|-------------|
| 最低付费价格 | $4.99/月 | $15/月 | $0(需Office) |
| 免费版可用性 | 3次/月 | 有限试用 | 完全免费 |
| iOS原生App | ✅ | ❌(Web) | ❌(桌面) |
| 离线使用 | ✅ | ❌ | ✅ |
| 一次性买断 | $79.99 | ❌ | N/A |
| 隐私保护 | ✅本地处理 | ❌云端 | ✅本地 |
| 学习曲线 | 极低 | 低 | 高 |

---

## 八、符合美国市场使用习惯的UI设计

### 8.1 设计原则

| 原则 | 说明 | 参考App |
|------|------|---------|
| 极简主义 | 每屏只做一件事，减少认知负荷 | Things 3, Bear |
| 渐进式披露 | 复杂功能按需展示，不一次性呈现 | Apple Settings |
| 即时反馈 | 每个操作都有视觉/触觉反馈 | Apple Pay |
| 手势优先 | 滑动操作代替按钮点击 | Mail, Messages |
| 深色模式 | 原生支持深色模式 | 所有Apple App |

### 8.2 色彩系统

```
主色调 (Primary)
├── Blue #007AFF (系统蓝 — 信任、专业、效率)
│   ├── Light: #4DA3FF
│   └── Dark: #0055CC

语义色彩 (Semantic)
├── 成功/修复: Green #34C759
├── 警告/需审核: Orange #FF9500
├── 错误/重复: Red #FF3B30
├── 信息/提示: Blue #5AC8FA
└── 中性: Gray #8E8E93

背景色
├── Light Mode: #F2F2F7 (系统灰)
└── Dark Mode: #1C1C1E (系统深灰)
```

### 8.3 核心页面设计

#### 页面1: 首页/文件导入

```
┌─────────────────────────────────┐
│  SheetSweep              [⚙️]   │
│                                  │
│  ┌─────────────────────────────┐│
│  │                             ││
│  │     📊                      ││
│  │                             ││
│  │   Drop your spreadsheet    ││
│  │   here to get started      ││
│  │                             ││
│  │   or tap to browse files   ││
│  │                             ││
│  │   Supports .xlsx .csv .xls ││
│  │                             ││
│  └─────────────────────────────┘│
│                                  │
│  Recent Files                    │
│  ┌─────────────────────────────┐│
│  │ 📄 supplier_q1.xlsx   3/28 ││
│  │    23 issues · Fixed        ││
│  ├─────────────────────────────┤│
│  │ 📄 vendor_list.csv    3/25 ││
│  │    12 issues · Fixed        ││
│  ├─────────────────────────────┤│
│  │ 📄 products_march.xlsx 3/20││
│  │    45 issues · Pending      ││
│  └─────────────────────────────┘│
│                                  │
│         [+ New File]             │
└─────────────────────────────────┘
```

#### 页面2: 扫描结果概览

```
┌─────────────────────────────────┐
│  ← Back    Scan Results    [📤] │
│                                  │
│  supplier_q1.xlsx               │
│  2,340 rows · 8 columns · 3 sheets│
│                                  │
│  ┌─────────────────────────────┐│
│  │  🎯 318 Issues Found        ││
│  │  Estimated save: 3.5 hours  ││
│  └─────────────────────────────┘│
│                                  │
│  Critical                        │
│  ┌─────────────────────────────┐│
│  │ 🔴 Duplicate Rows      23  ││
│  │    Same data across sheets  ││
│  ├─────────────────────────────┤│
│  │ 🟠 Merged Cells       12   ││
│  │    Breaking filters/sort    ││
│  └─────────────────────────────┘│
│                                  │
│  Warnings                        │
│  ┌─────────────────────────────┐│
│  │ 🟡 Date Formats      156   ││
│  │    5 different formats found││
│  ├─────────────────────────────┤│
│  │ 🟡 Column Names        4   ││
│  │    Non-standard headers     ││
│  ├─────────────────────────────┤│
│  │ 🟣 Currency Mixed     34   ││
│  │    USD, EUR, GBP detected   ││
│  └─────────────────────────────┘│
│                                  │
│  Info                            │
│  ┌─────────────────────────────┐│
│  │ 🔵 Extra Spaces       89   ││
│  │    Trailing/leading spaces  ││
│  └─────────────────────────────┘│
│                                  │
│  [⚡ Fix All]    [🔍 Review Each]│
└─────────────────────────────────┘
```

#### 页面3: 问题审核详情（重复行）

```
┌─────────────────────────────────┐
│  ← Back   Duplicates      [✅]  │
│                                  │
│  23 duplicate groups found       │
│  Showing group 1 of 23           │
│                                  │
│  Similarity: 94%                 │
│  ┌─────────────────────────────┐│
│  │  Row 45 (keep ✓)            ││
│  │  SKU: WD-4420              ││
│  │  Name: Widget Deluxe Pro    ││
│  │  Price: $24.99              ││
│  │  Stock: 150                 ││
│  │  Category: Hardware         ││
│  └─────────────────────────────┘│
│  ┌─────────────────────────────┐│
│  │  Row 67                     ││
│  │  SKU: WD4420               ││
│  │  Name: Widget Deluxe Pro.   ││
│  │  Price: $24.99              ││
│  │  Stock: —                   ││
│  │  Category: hardware         ││
│  └─────────────────────────────┘│
│                                  │
│  [Keep Row 45]  [Keep Row 67]   │
│  [Merge Both]                    │
│                                  │
│  ◀ Previous    Next ▶           │
└─────────────────────────────────┘
```

#### 页面4: 修复完成 + 导出

```
┌─────────────────────────────────┐
│  ✅ All Fixed!                   │
│                                  │
│  ┌─────────────────────────────┐│
│  │                             ││
│  │         🎉                  ││
│  │                             ││
│  │   318 issues resolved       ││
│  │   ~3.5 hours saved          ││
│  │                             ││
│  └─────────────────────────────┘│
│                                  │
│  Summary                         │
│  ✅ 23 duplicates removed        │
│  ✅ 156 dates standardized       │
│  ✅ 4 columns renamed            │
│  ✅ 12 merged cells split        │
│  ✅ 89 spaces cleaned            │
│  ✅ 34 currencies unified        │
│                                  │
│  ┌─────────────────────────────┐│
│  │  💾 Export as XLSX    (Pro) ││
│  ├─────────────────────────────┤│
│  │  📄 Export as CSV           ││
│  ├─────────────────────────────┤│
│  │  📋 Copy to Clipboard      ││
│  ├─────────────────────────────┤│
│  │  📧 Share via Email        ││
│  └─────────────────────────────┘│
│                                  │
│  [💾 Save Template for Supplier] │
└─────────────────────────────────┘
```

### 8.4 关键交互设计

| 交互 | 手势/操作 | 反馈 |
|------|----------|------|
| 导入文件 | 拖拽到App / 点击"+"按钮 | 触觉反馈 + 进度动画 |
| 查看问题详情 | 点击问题卡片 | 卡片展开动画 + 数值高亮 |
| 确认修复 | 滑动确认 / 点击确认按钮 | 绿色✅动画 + 触觉反馈 |
| 拒绝修复 | 左滑删除 | 红色删除动画 |
| 批量操作 | 长按选择 → 顶部操作栏 | 多选模式 + 计数徽章 |
| 导出文件 | 点击导出按钮 → 选择格式 | 分享面板弹出 |

### 8.5 动画规范

| 动画 | 时长 | 曲线 | 用途 |
|------|------|------|------|
| 页面转场 | 0.35s | easeInOut | NavigationStack push/pop |
| 卡片展开 | 0.25s | spring | 问题详情展开 |
| 进度更新 | 0.15s | linear | 扫描进度条 |
| 修复确认 | 0.3s | spring | ✅确认动画 |
| 错误抖动 | 0.4s | spring | 输入错误反馈 |

---

## 九、MVP开发计划

### Phase 1: MVP（3-4周）

| 周 | 任务 | 交付物 |
|---|------|--------|
| W1 | 文件解析层 + 数据模型 | XLSX/CSV解析、SwiftData模型 |
| W2 | 问题检测 + 去重引擎 + 格式标准化 | 核心处理逻辑 |
| W3 | UI层 + 审核流程 + 导出功能 | 完整用户流程 |
| W4 | 测试 + 优化 + App Store提交 | 上架版本 |

### Phase 2: 增强（2-3周）

| 功能 | 描述 |
|------|------|
| 供应商模板记忆 | 保存每个供应商的清理规则，下次自动应用 |
| 批量文件处理 | 一次导入多个文件，批量清理 |
| 跨文件去重 | 多个供应商文件之间去重 |
| 自定义Schema | 用户自定义标准列名映射 |

### Phase 3: 高级功能（2-3周）

| 功能 | 描述 |
|------|------|
| 清理报告PDF导出 | 生成专业数据质量报告 |
| iCloud规则同步 | 多设备同步清理规则 |
| Apple Watch快速预览 | 手表端查看清理状态 |
| Widget小组件 | 桌面快速导入文件 |

---

## 十、技术架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    SheetSweep 技术架构                    │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  UI Layer (SwiftUI)                                      │
│  ├── HomeView (文件导入 + 历史记录)                       │
│  ├── ScanResultView (问题概览)                            │
│  ├── IssueDetailView (问题审核)                           │
│  ├── ExportView (导出选项)                                │
│  └── SettingsView (偏好设置)                              │
│                                                           │
│  ViewModel Layer (@Observable)                           │
│  ├── CleaningViewModel (主流程协调)                       │
│  ├── ScanViewModel (扫描状态管理)                         │
│  └── ExportViewModel (导出状态管理)                       │
│                                                           │
│  Business Logic Layer (Actor)                            │
│  ├── FileParserService (文件解析调度)                     │
│  ├── IssueScannerService (问题检测)                       │
│  ├── DeduplicationEngine (去重引擎)                       │
│  ├── FormatStandardizer (格式标准化)                      │
│  ├── ColumnNameNormalizer (列名规范化)                    │
│  └── CleaningOrchestrator (清理流程编排)                  │
│                                                           │
│  Data Layer (SwiftData)                                  │
│  ├── CleaningSession (会话模型)                           │
│  ├── DataIssue (问题模型)                                 │
│  ├── CleaningRule (规则模型)                              │
│  ├── ColumnMapping (列映射模型)                           │
│  └── SupplierTemplate (供应商模板)                        │
│                                                           │
│  External Dependencies (SPM)                             │
│  ├── CoreXLSX (XLSX解析)                                 │
│  ├── SwiftCSV (CSV解析)                                  │
│  ├── ZIPFoundation (ZIP处理)                              │
│  └── SwiftDate (日期处理)                                 │
│                                                           │
│  Platform                                                 │
│  ├── iOS 17+ / iPadOS 17+ / macOS 14+ (Apple Silicon)   │
│  ├── SwiftData (本地持久化)                               │
│  ├── Swift Concurrency (async/await, Actor)              │
│  └── UniformTypeIdentifiers (文件类型识别)                │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

---

## 十一、App Store上架策略

### 11.1 关键词优化

**标题**: SheetSweep - Spreadsheet Cleaner  
**副标题**: Clean Messy Spreadsheets Fast  
**关键词**: spreadsheet,excel,clean,duplicate,format,CSV,XLSX,vendor,supplier,data,merge,standardize,dedupe,fix

### 11.2 截图策略（6.7" iPhone）

| 截图 | 内容 | 标题 |
|------|------|------|
| 1 | 拖拽导入文件 | "Import Any Spreadsheet" |
| 2 | 扫描结果概览 | "Find 318 Issues in Seconds" |
| 3 | 重复行审核 | "Smart Duplicate Detection" |
| 4 | 一键修复 | "Fix Everything with One Tap" |
| 5 | 导出选项 | "Export Clean Data Instantly" |

### 11.3 App描述

```
SheetSweep cleans messy vendor spreadsheets in seconds — not hours.

Every time you receive a supplier Excel file, it's the same nightmare: 
duplicate rows, broken date formats, merged cells, extra spaces that 
kill your lookups, and column names that never match.

SheetSweep fixes all of that automatically:

• 🔍 Smart Duplicate Detection — Exact + fuzzy matching finds duplicates even with typos
• 📅 Date Format Standardization — Unifies MM/DD, DD/MM, text dates into one format
• 🏷️ Column Name Normalization — Maps "Product ID" / "SKU" / "Item Code" to one standard
• 🧹 Space & Hidden Character Cleanup — Removes trailing spaces and invisible characters
• 💱 Currency Format Unification — Standardizes mixed currencies (USD, EUR, GBP)
• 📤 One-Tap Export — Clean XLSX or CSV ready for your ERP/CRM

🔒 100% Private — All processing happens on your device. No cloud uploads, no data collection.

Perfect for:
• Procurement teams cleaning vendor spreadsheets
• Finance departments preparing data for ERP import
• Small businesses managing multiple supplier files
• Anyone tired of spending hours fixing messy Excel data

Try it free — clean 3 spreadsheets per month at no cost.

---

### 11.4 App分类

| 项目 | 选择 |
|------|------|
| Primary Category | Business |
| Secondary Category | Productivity |
| Age Rating | 4+ |

---

## 十二、竞争优势总结

### SheetSweep vs 现有解决方案

| 维度 | SheetSweep | RowTidy | Power Query | OpenRefine |
|------|-----------|---------|-------------|------------|
| 平台 | iOS原生 | Web | Win/Mac桌面 | 桌面 |
| 价格 | $4.99/月起 | $15/月起 | 免费(需Office) | 免费 |
| 学习曲线 | 极低(1分钟上手) | 低 | 高 | 高 |
| 隐私 | 100%本地 | 云端处理 | 本地 | 本地 |
| 离线使用 | ✅ | ❌ | ✅ | ✅ |
| 移动端 | ✅原生 | ❌ | ❌ | ❌ |
| 模糊去重 | ✅JaroWinkler | ✅AI | ❌ | ❌ |
| 供应商模板 | ✅ | ✅ | ❌ | ❌ |
| 一键修复 | ✅ | ✅ | ❌ | ❌ |
| XLSX导出 | ✅ | ✅ | N/A | ✅ |

### 核心差异化优势

1. **唯一iOS原生电子表格清理App** — App Store无直接竞品
2. **价格仅为RowTidy的1/3** — $4.99 vs $15/月
3. **100%本地处理** — 隐私优先，无需上传数据到云端
4. **JaroWinkler模糊匹配** — 精确识别"ABC Inc" vs "ABC Incorporated"类重复
5. **供应商模板记忆** — 同一供应商下次自动应用规则
6. **一键修复+逐项审核** — 灵活选择，满足不同场景需求
7. **离线可用** — 外出、飞机上也能处理文件

---

## 十三、风险与应对

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|---------|
| CoreXLSX库停止维护 | 低 | 高 | Fork仓库自行维护；备选方案：自建XLSX解析器 |
| 大文件性能问题 | 中 | 中 | 流式解析+分块处理；限制免费版行数 |
| App Store审核被拒 | 低 | 高 | 确保隐私政策完整；无隐藏数据收集 |
| RowTidy推出iOS版 | 低 | 高 | 先发优势+价格优势+本地处理差异化 |
| 用户不愿为工具付费 | 中 | 中 | 免费版3次/月体验核心价值；强调节省时间成本 |

---

*本文档由任意LLM均可阅读并复刻出完整的SheetSweep iOS应用。所有核心技术代码、架构设计、UI规范、定价策略均已详细描述。*